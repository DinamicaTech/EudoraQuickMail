<Window x:Class="QuickMail.Views.SlowOperationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Working" Width="430" Height="145" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource Theme.WindowBackground}"
        Foreground="{DynamicResource Theme.TextPrimary}"
        AutomationProperties.Name="Operation in progress">
    <Border Padding="18" BorderBrush="{DynamicResource Theme.Border}" BorderThickness="1">
        <StackPanel>
            <TextBlock x:Name="OperationTitle" FontWeight="SemiBold"
                       FontSize="{DynamicResource Theme.FontSizeHeader}"
                       TextWrapping="Wrap"/>
            <ProgressBar Height="8" Margin="0,16,0,10" IsIndeterminate="True"
                         AutomationProperties.Name="Operation progress"/>
            <TextBlock x:Name="OperationStatus" Text="Working…"
                       Foreground="{DynamicResource Theme.TextSecondary}" TextWrapping="Wrap"/>
        </StackPanel>
    </Border>
</Window>
