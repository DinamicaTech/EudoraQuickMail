<Window x:Class="QuickMail.Views.RulesManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:QuickMail.Views"
        Title="{Binding WindowTitle}"
        AutomationProperties.Name="{Binding WindowTitle}"
        Width="750" Height="874"
        MinWidth="600" MinHeight="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource Theme.WindowBackground}"
        Foreground="{DynamicResource Theme.TextPrimary}"
        FontFamily="{DynamicResource Theme.FontFamily}"
        FontSize="{DynamicResource Theme.FontSizeBase}">

    <Window.Resources>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <Style TargetType="TextBox" x:Key="RuleTextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="Margin" Value="0,2,0,8"/>
            <Setter Property="Padding" Value="4,3"/>
            <!-- MinHeight, never Height: a fixed height clipped the text at large
                 text scales (#449). -->
            <Setter Property="MinHeight" Value="26"/>
        </Style>
        <Style TargetType="ComboBox" x:Key="RuleComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
            <Setter Property="Margin" Value="0,2,0,8"/>
            <!-- MinHeight, never Height: a fixed height clipped the text at large
                 text scales (#449). -->
            <Setter Property="MinHeight" Value="26"/>
        </Style>
        <Style TargetType="CheckBox" x:Key="RuleCheckBox" BasedOn="{StaticResource {x:Type CheckBox}}">
            <Setter Property="Margin" Value="0,4,0,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="6"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left pane: Rule list -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <WrapPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                    <Button Content="New Rule" Command="{Binding NewRuleCommand}"
                            MinWidth="80" Margin="0,0,6,6"
                            AutomationProperties.Name="Create new rule"/>
                    <Button Content="Delete" Command="{Binding DeleteRuleCommand}"
                            MinWidth="60" Margin="0,0,6,6"
                            AutomationProperties.Name="Delete selected rule"/>
                    <Button Content="Run on Existing Mail" Command="{Binding RunOnExistingCommand}" Padding="8,0" Margin="0,0,0,6"
                            AutomationProperties.Name="Run all rules on existing mail now"/>
                    <Button Content="Cleanup" Command="{Binding CleanupCommand}" Padding="8,0" Margin="0,0,0,6"
                            AutomationProperties.Name="Remove filters whose target folder does not exist"/>
                </WrapPanel>

                <Grid Grid.Row="1" Margin="0,0,0,6">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <CheckBox Content="See all filters"
                                  IsChecked="{Binding SeeAllFilters}"
                                  Visibility="{Binding HasCompatibilityMessage, Converter={StaticResource BoolToVisibilityConverter}}"
                                  Margin="0,0,8,4"
                                  AutomationProperties.Name="See all filters"/>
                        <Button Content="Search Filter" Click="SearchFilterButton_Click"
                                Padding="8,0" HorizontalAlignment="Left"
                                AutomationProperties.Name="Search filters by message criteria"/>
                    </StackPanel>
                </Grid>

                <ListBox Grid.Row="2"
                         x:Name="RuleListBox"
                         ItemsSource="{Binding RulesView}"
                         SelectedItem="{Binding SelectedRule}"
                         AutomationProperties.Name="Rules list">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="6,4"/>
                            <!-- The screen reader reads the row from the ListBoxItem's own Name, not
                                 from a child element's AutomationProperties.Name (verified by
                                 SelectorItemAccessibilityTests against the live peer). So the composed
                                 "name, account" (honouring the field-labels setting) must sit on the
                                 container. The template below is visual only. -->
                            <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}"/>
                                <TextBlock Text="{Binding AccountDisplay}"
                                           Opacity="0.6" Margin="12,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch"
                          Background="Transparent"
                          KeyboardNavigation.IsTabStop="False"
                          Focusable="False"/>

            <!-- Right pane: Rule editor -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto"
                          Focusable="False"
                          KeyboardNavigation.TabNavigation="Continue"
                          KeyboardNavigation.IsTabStop="False">
                <StackPanel Margin="12,0,0,0">
                  <!-- Fields disabled when no rule is selected (e.g. no rules yet): they bind to
                       SelectedRule.* and would otherwise be live-but-blank, so a keyboard/screen-reader
                       user could tab onto an empty Account combo with nothing behind it. The button row
                       below stays OUTSIDE this so Close is always available; Test/Save gate themselves
                       via CanExecute. -->
                  <StackPanel IsEnabled="{Binding HasSelectedRule}">
                    <!-- Rule Name -->
                    <TextBlock Text="Rule Name" FontWeight="SemiBold" Margin="0,8,0,2"/>
                    <TextBox Text="{Binding SelectedRule.Name, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RuleTextBox}"
                             AutomationProperties.Name="Rule name"/>
                    <TextBlock Text="{Binding NameError}" Foreground="{DynamicResource Theme.Error}"
                               KeyboardNavigation.IsTabStop="False"
                               AutomationProperties.LiveSetting="Assertive">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding NameError}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding NameError}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Enabled -->
                    <CheckBox Content="Enabled"
                              IsChecked="{Binding SelectedRule.IsEnabled}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Rule enabled"/>

                    <CheckBox Content="Apply automatically to incoming mail"
                              IsChecked="{Binding SelectedRule.ApplyAutomatically}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Apply rule automatically to incoming mail"/>

                    <CheckBox Content="Also filter Out mailbox when run manually"
                              IsChecked="{Binding SelectedRule.AlsoFilterOutMailbox}"
                              Style="{StaticResource RuleCheckBox}"
                              ToolTip="When this rule is run manually, also apply it to messages currently in the physical Out mailbox. It does not enable automatic processing."
                              AutomationProperties.Name="Also filter Out mailbox when run manually"/>

                    <!-- Conditions -->
                    <TextBlock Text="Conditions" FontWeight="SemiBold" Margin="0,12,0,4"/>
                    <Separator Margin="0,0,0,6"/>

                    <TextBlock Text="Account"/>
                    <ComboBox x:Name="AccountScopeCombo"
                              ItemsSource="{Binding AccountOptions}"
                              SelectedValue="{Binding SelectedRule.AccountId}"
                              SelectedValuePath="Id"
                              Style="{StaticResource RuleComboBox}"
                              AutomationProperties.Name="Account scope"/>

                    <!-- From condition -->
                    <CheckBox Content="From"
                              IsChecked="{Binding IsFromEnabled}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Use From condition"/>
                    <TextBox x:Name="FromContainsTextBox"
                             Text="{Binding SelectedRule.FromContains, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RuleTextBox}"
                             PreviewMouseDoubleClick="FromContainsTextBox_MouseDoubleClick"
                             IsReadOnly="{Binding IsFromEnabled, Converter={StaticResource InverseBoolConverter}, FallbackValue=True}"
                             IsTabStop="{Binding IsFromEnabled}"
                             AutomationProperties.Name="From contains"
                             AutomationProperties.HelpText="e.g., manager@company.com or just company.com"/>

                    <!-- To condition -->
                    <StackPanel Orientation="Horizontal">
                        <CheckBox Content="To"
                                  IsChecked="{Binding IsToEnabled}"
                                  Style="{StaticResource RuleCheckBox}"
                                  AutomationProperties.Name="Use To condition"/>
                        <CheckBox Content="Also CC/BCC"
                                  Margin="18,0,0,0"
                                  VerticalAlignment="Center"
                                  IsEnabled="{Binding IsToEnabled}"
                                  IsChecked="{Binding SelectedRule.AlsoCcBcc, Mode=TwoWay}"
                                  AutomationProperties.Name="Also search CC and BCC recipients"/>
                    </StackPanel>
                    <TextBox Text="{Binding SelectedRule.ToContains, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RuleTextBox}"
                             IsReadOnly="{Binding IsToEnabled, Converter={StaticResource InverseBoolConverter}, FallbackValue=True}"
                             IsTabStop="{Binding IsToEnabled}"
                             AutomationProperties.Name="To contains"
                             AutomationProperties.HelpText="e.g., team@company.com"/>

                    <!-- Subject condition -->
                    <CheckBox Content="Subject"
                              IsChecked="{Binding IsSubjectEnabled}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Use Subject condition"/>
                    <TextBox Text="{Binding SelectedRule.SubjectContains, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RuleTextBox}"
                             IsReadOnly="{Binding IsSubjectEnabled, Converter={StaticResource InverseBoolConverter}, FallbackValue=True}"
                             IsTabStop="{Binding IsSubjectEnabled}"
                             AutomationProperties.Name="Subject contains"
                             AutomationProperties.HelpText="e.g., Weekly Report"/>

                    <!-- Body condition -->
                    <CheckBox Content="Body"
                              IsChecked="{Binding IsBodyEnabled}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Use Body condition"/>
                    <TextBox Text="{Binding SelectedRule.BodyContains, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource RuleTextBox}"
                             IsReadOnly="{Binding IsBodyEnabled, Converter={StaticResource InverseBoolConverter}, FallbackValue=True}"
                             IsTabStop="{Binding IsBodyEnabled}"
                             AutomationProperties.Name="Body contains"
                             AutomationProperties.HelpText="Text to search in message body preview"/>

                    <CheckBox Content="Has attachments"
                              IsChecked="{Binding SelectedRule.MustHaveAttachments}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Must have attachments"/>

                    <TextBlock Text="{Binding ConditionsError}" Foreground="{DynamicResource Theme.Error}"
                               KeyboardNavigation.IsTabStop="False"
                               AutomationProperties.LiveSetting="Assertive">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding ConditionsError}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding ConditionsError}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Action -->
                    <TextBlock Text="Action" FontWeight="SemiBold" Margin="0,12,0,4"/>
                    <Separator Margin="0,0,0,6"/>

                    <ComboBox x:Name="ActionCombo"
                              ItemsSource="{Binding ActionOptions}"
                              SelectedValue="{Binding SelectedRule.Action}"
                              SelectedValuePath="Action"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource RuleComboBox}"
                              AutomationProperties.Name="Rule action"/>

                    <CheckBox Content="Also mark as read"
                              IsChecked="{Binding SelectedRule.AlsoMarkAsRead}"
                              Style="{StaticResource RuleCheckBox}"
                              AutomationProperties.Name="Also mark matching messages as read"/>

                    <!-- Target folder -->
                    <TextBlock Text="Target folder"/>
                    <!-- Accessible name tracks the chosen folder (issue #344): a static name made
                         a screen reader always say "Choose target folder" even after a folder was
                         picked. PickFolder raises PropertyChanged(SelectedRule), refreshing both. -->
                    <Button Content="{Binding SelectedRule.TargetFolder, TargetNullValue=Choose Folder…, FallbackValue=Choose Folder…}"
                            Command="{Binding PickFolderCommand}"
                            MinWidth="140" Margin="0,2,0,8"
                            HorizontalAlignment="Left"
                            AutomationProperties.Name="{Binding SelectedRule.TargetFolder, TargetNullValue=Choose target folder, FallbackValue=Choose target folder}"/>
                    <TextBlock Text="{Binding FolderError}" Foreground="{DynamicResource Theme.Error}"
                               KeyboardNavigation.IsTabStop="False"
                               AutomationProperties.LiveSetting="Assertive">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding FolderError}" Value="">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding FolderError}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                  </StackPanel>
                  <!-- End of the fields panel; buttons below stay enabled. -->

                    <!-- Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="Test Rule" Command="{Binding TestRuleCommand}"
                                MinWidth="80" Margin="0,0,8,0"
                                AutomationProperties.Name="Test rule against selected messages"/>
                        <Button Content="{Binding ApplyToCurrentFolderLabel}"
                                Command="{Binding ApplyToCurrentFolderCommand}"
                                MinWidth="110" Margin="0,0,8,0"
                                AutomationProperties.Name="{Binding ApplyToCurrentFolderLabel}"
                                ToolTip="Save this rule, apply it to every message in the current folder, and close"/>
                        <Button Content="Save" Command="{Binding SaveRuleCommand}"
                                MinWidth="60" Margin="0,0,8,0"
                                AutomationProperties.Name="Save rule"
                                IsDefault="True"/>
                        <Button Content="Cancel" Command="{Binding CancelCommand}"
                                MinWidth="60"
                                AutomationProperties.Name="Cancel rule changes"
                                IsCancel="True"/>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Status bar -->
        <StatusBar Grid.Row="1" Margin="0,8,0,0">
            <StatusBarItem>
                <TextBlock x:Name="MainStatusText" Text="{Binding StatusText}"
                           Focusable="True" KeyboardNavigation.IsTabStop="False"
                           AutomationProperties.Name="{Binding StatusText}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
